#include<stdio.h>

// function declaration only!
int sum(int num1, int num2);

// function declaration + its definition
// A function to add two numbers and return their sum
int sum(int num1, int num2)
{
    return (num1 + num2);
}